#include<bits/stdc++.h>
using namespace std;
#ifdef LOCAL
#include<debug.h>
#define local(...) do {__VA_ARGS__;} while (0)
#else
#define local(...) do {} while (0)
#endif

int main() {
    int n = 10;  // Just an example, make sure to define 'n' in your actual code.
    dbg(__init__());
    debug(
        for(int i = 0; i < n; ++i) {
            std::cout << i << " ";
            if(1){
                if(1){
                    cout << 1 << endl;
                }
            }
        }
    );

    return 0;
}